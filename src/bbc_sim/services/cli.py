"""Client CLI commands (whois/read-property/...). Registered into the root Typer app."""

from __future__ import annotations

import typer


def register(app: typer.Typer) -> None:
    """Register client commands. Implemented in EP-001.8."""
    return None
